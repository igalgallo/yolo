apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
spec:
  selector:
    app: mongodb
  ports:
    - protocol: TCP
      port: 27017  # MongoDB default port
      targetPort: 27017  # Port your MongoDB container is listening on